(defvar lotsohuggin-scratch-mode 'text-mode)

(defvar lotsohuggin-leader-key "SPC")

(defvar lotsohuggin-general-non-normal-prefix "M-SPC")

(defvar lotsohuggin-major-mode-leader-key ",")

(defvar lotsohuggin-ex-command-key ":")

(defvar lotsohuggin-command-key "SPC")

(defvar lotsohuggin-which-key-delay 0.3)

(defvar lotsohuggin-keep-backups nil)

(defvar lotsohuggin-helm-position 'bottom)

;; directory where all my org files are stored
(defconst lotsohuggin-org-dir "~/Google\sDrive/org")

(provide 'init-config-variables)
